// const Router = require('koa-router')
// let page = new Router()
// page.get('/404', (ctx)=>{
//     ctx.body = '404 page!'
// })
// page.get('/hello', (ctx)=>{
//     ctx.body = 'hello page!'
// })

// let router = new Router()
// page.use('/', home.routes(), home.allowedMethods())
// page.use('/page', page.routes(), page.allowedMethods())
const http = require('http')

class Router {

};


module.exports = Router;